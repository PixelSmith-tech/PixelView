<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:colorBackground"> <!-- Используем фон из темы -->

    <ListView
        android:id="@+id/settingsTabs"
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:divider="@null"
        android:background="?attr/colorSurface" /> <!-- Чуть светлее фон из темы -->

    <FrameLayout
        android:id="@+id/settingsContent"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:background="?android:colorBackground"/> <!-- Общий фон из темы -->
</LinearLayout>
